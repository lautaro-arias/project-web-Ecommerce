'use client'
//import { useProviderGlobal } from '@/app/provider/useGlobalProvider';
//import { usePathname } from 'next/navigation';
import React from 'react'

export default function AddCart() {

 /* const { handleAddCart } = useProviderGlobal()
  const pathname = usePathname(); */

  return (
    <>
      {/*pathname === '/' &&
        <button
          onClick={() => { item ? alert('Añadido al carro') : alert('No hay Stock'); handleAddCart(item) }}
          className="inline-flex mb-4 text-white bg-green-700 border-0 py-2 px-6 focus:outline-none hover:bg-green-800 rounded text-md">
          Añadir al carro +
        </button>
      */}
    </>
  )
}
